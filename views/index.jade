doctype html
html
  head
    title Sample ToDo with RiotJS and Redux and Jade/Stylus tags
  body
    | !{initialRender}
    
    script(src="/js/lib/riot.js")
    script(src="/tags")
    script.
      riot.mount('*', { firstName: '#{firstName}' })
