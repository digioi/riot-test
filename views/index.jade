doctype html
html
  head
    title Sample ToDo with RiotJS and Redux and Jade/Stylus tags
  body
    | !{initialRender}
    script(src="vendor.js")
    script.
      window.__INITIAL_STATE__ = !{JSON.stringify(initialState)}
    script(src="todo.js")
